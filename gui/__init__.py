"""Graphical interface package for the AnimeSaturn Downloader.

A self-contained desktop GUI (PySide6) that searches/browses AnimeSaturn directly,
lists an anime's episodes and downloads the direct ``.mp4`` files with progress
tracking and resume. The package has no external engine dependency so it can be
packaged into a standalone Windows executable.
"""

__all__ = ["__version__"]

__version__ = "1.6.2"
